apiVersion: v1
kind: Service
metadata:
  name: {{ include "arr_stack.fullname" . }}
  labels:
    {{- include "arr_stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arr_stack.selectorLabels" . | nindent 4 }}
