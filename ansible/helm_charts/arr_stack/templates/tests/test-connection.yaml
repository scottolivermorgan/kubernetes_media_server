apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arr_stack.fullname" . }}-test-connection"
  labels:
    {{- include "arr_stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arr_stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
